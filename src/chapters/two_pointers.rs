//! in problems where we deal with sorted arrays(or linked lists) and need to find a set of
//! elements that fulfill certain constraints, the Two Pointers approach becomes quite useful.
//!
//! the set of elements could be a pair, a triplet or even a subarray
